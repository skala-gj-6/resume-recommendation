# docker compose 없이 `npm run dev`로 단독 실행할 때만 필요합니다.
# docker compose 환경에서는 compose.yaml이 이 값을 자동으로 주입합니다.

# Mock Recruitment Provider API (비로그인 공고·추천 원본). 브라우저가 직접 호출합니다.
VITE_MOCK_API_BASE_URL=http://localhost:8000
